# The GitHub PAT is a real credential -- never commit it.
secrets/

# cloudflared tunnel credentials (TunnelSecret, cert.pem) -- never commit
cloudflared/

# .dsh-context contains credentials, sessions, and other sensitive data
.dsh-context/

# but settings.yaml is needed for the image build
!.dsh-context/settings.yaml