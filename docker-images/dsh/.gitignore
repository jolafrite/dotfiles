# The GitHub PAT is a real credential -- never commit it.
secrets/

# cloudflared tunnel credentials (TunnelSecret, cert.pem) -- never commit
cloudflared/

# .dsh-context: ignore sensitive subdirs only
.dsh-context/.credentials.yaml
.dsh-context/attachments/
.dsh-context/integrations/
.dsh-context/remote-web-ui-devices.json
.dsh-context/remote-web-ui-registry/
.dsh-context/sessions/
.dsh-context/storages/
.dsh-context/profiles/